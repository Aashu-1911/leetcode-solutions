class Solution {
public:

    int f(int i,int j,vector<int>&nums){
        if(i == j){
            return nums[i];
        }

        int takeleft = nums[i] - f(i+1,j,nums);
        int takeright = nums[j] - f(i,j-1,nums);

        return max(takeright, takeleft);
    }

    bool predictTheWinner(vector<int>& nums) {