            if(s<target)i++;
            if(s>target)j--;
            else if(s == target){
                ans.push_back(i+1);
            }
                ans.push_back(j+1);
                break;
        }
    }
        return ans;
        int j= numbers.size()-1;
        while(i<j){
            int s = numbers[i]+numbers[j];
};